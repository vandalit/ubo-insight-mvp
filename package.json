{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "cd frontend && npm ci && npm run build && cd .. && cp -r frontend/dist/frontend/browser/* public/",
        "dev": "vite dev",
        "heroku-postbuild": "npm run build"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "autoprefixer": "^10.4.2",
        "laravel-vite-plugin": "^1.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.1.0",
        "vite": "^6.0.0"
    },
    "engines": {
        "node": "20.x",
        "npm": "10.x"
    }
}
